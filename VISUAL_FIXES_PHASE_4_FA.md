# گزارش اصلاحات ظاهری مرحله چهارم ELORIA

## محدوده این مرحله

این مرحله بر تکمیل صفحه اصلی، پیوستگی روایت برند، دسترسی سریع به گنجینه‌ها و کاهش اصطکاک Intro تمرکز دارد.

## تغییرات انجام‌شده

### صفحه اصلی

- اضافه‌شدن بخش کامل گنجینه‌های گردنبند، دستبند و گوشواره بعد از Hero.
- استفاده از تصاویر واقعی موجود در پروژه برای کارت‌های گنجینه.
- اضافه‌شدن دکمه مشاهده همه محصولات.
- اضافه‌شدن بخش فلسفه طراحی و معرفی هویت الــوریا.
- اضافه‌شدن بخش اعتماد شامل قیمت‌گذاری شفاف، جزئیات قابل بررسی و تجربه دو‌زبانه.
- اضافه‌شدن CTA نهایی برای ورود به فروشگاه یا مطالعه داستان برند.
- ایجاد فاصله‌گذاری، پس‌زمینه، کنتراست و مرزبندی یکپارچه میان بخش‌های صفحه اصلی.

### Hero و ناوبری

- اتصال دکمه اصلی Hero به بخش گنجینه‌های همان صفحه.
- تبدیل متن پایین Hero به راهنمای اسکرول قابل‌کلیک.
- اضافه‌شدن لینک گنجینه‌ها و تمام محصولات به منوی ابتدایی صفحه اصلی.
- تکمیل متن توضیح منو در نسخه فارسی و انگلیسی.

### Intro

- Intro در هر بار ورود یا بارگذاری مجدد صفحه اصلی اجرا می‌شود.
- وضعیت تکمیل Intro در مرورگر ذخیره نمی‌شود.

## فایل‌های اصلی تغییرکرده

- `src/app/[locale]/page.tsx`
- `src/components/home-showcase-sections.tsx`
- `src/components/hero-showcase.tsx`
- `src/components/home-header-controller.tsx`
- `src/components/eloria-intro-experience.tsx`

## تست انجام‌شده

- تمام فایل‌های TypeScript و TSX از نظر خطاهای نحوی بررسی شدند.
- Markerهای Merge Conflict و فایل‌های ناخواسته بررسی شدند.
- تفاوت نسخه جدید با Phase 3 کنترل شد و فقط فایل‌های موردنیاز تغییر کرده‌اند.

## محدودیت محیط بررسی

نصب کامل Dependencyها در محیط بررسی به‌دلیل نبود پکیج `zod-validation-error@4.0.2` در Registry داخلی متوقف شد. بنابراین Build نهایی باید روی سیستم پروژه با دستورات زیر اجرا شود:

```powershell
npm install
npm run lint
npm run build
```
