# ELORIA — مرحله ۷.۰: پایه پنل مدیریت

این نسخه بدون تغییر Schema دیتابیس، یک پنل مدیریت محافظت‌شده به پروژه اضافه می‌کند.

## مسیر ورود

```text
/fa/admin/login
```

## امکانات

- ورود مدیر با Cookie امن، HttpOnly و SameSite Strict
- امضای نشست با HMAC و انقضای ۸ ساعته
- قفل‌شدن پنل در صورت نبود تنظیمات امنیتی
- داشبورد آمار محصولات، موجودی، سفارش‌ها و نرخ فلزات
- جست‌وجو و فیلتر محصولات
- ساخت محصول جدید
- ویرایش اطلاعات فارسی و انگلیسی محصول
- مدیریت قیمت دستی/پویا، وزن، عیار، اجرت، سود و مالیات
- مدیریت موجودی، وضعیت انتشار و اثر منتخب
- ثبت یا تغییر تصویر اصلی با URL
- مشاهده تنوع‌های فعلی محصول به‌صورت فقط خواندنی
- جست‌وجو و فیلتر سفارش‌ها
- مشاهده مشتری، نشانی، اقلام، پرداخت‌ها و Audit Log سفارش

## عمداً در این نسخه انجام نشده

- آپلود مستقیم تصویر و اتصال Storage
- ساخت و ویرایش تنوع‌ها
- تغییر وضعیت مالی/انبار سفارش از پنل
- اتصال درگاه پرداخت
- مدیریت چند مدیر و سطح دسترسی

این موارد باید با گردش‌کار کنترل‌شده در مراحل بعد اضافه شوند.

## تنظیم `.env`

فایل `.env` فعلی را باز کنید و این دو متغیر را اضافه کنید:

```env
ELORIA_ADMIN_PASSWORD="یک رمز قوی حداقل ۱۲ کاراکتری"
ELORIA_ADMIN_SESSION_SECRET="خروجی دستور npm run admin:secret"
```

برای تولید Secret:

```powershell
npm run admin:secret
```

خروجی را بدون فاصله داخل `ELORIA_ADMIN_SESSION_SECRET` قرار دهید و سرور را Restart کنید.
