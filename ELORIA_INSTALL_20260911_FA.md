# بسته اصلاح الوریا — 2026-09-11

این بسته سورس کامل اصلاح‌شده است، نه خروجی کامپایل‌شده یا تضمین پرداخت واقعی. TypeScript، ESLint و آزمون‌های شبیه‌سازی‌شده قیمت و پرداخت در محیط تهیه بسته موفق بودند. ساخت نهایی در این محیط با خطای داخلی SWC/stacker هنگام بارگذاری next.config.ts متوقف شد. نصب‌کننده روی رایانه شما ساخت را دوباره انجام می‌دهد و اگر موفق نشود، پروژه اصلی را جایگزین نمی‌کند. اسکریپت نصب در ویندوز اجرا نشده و منطق و محتوای بسته بررسی شده است.

## محتویات

- `source`: سورس کامل شامل تصاویر، فونت‌ها، فایل قفل وابستگی‌ها و اصلاحات.
- `INSTALL.ps1`: نصب با بررسی تفاوت نسخه، ساخت در پوشه جدا و نگهداری نسخه قبلی.
- `update-manifest.json`: مسیر و SHA256 فایل‌های تغییرکرده و نسخه مبنا.
- `ELORIA_UPDATE_REPORT_FA.md`: گزارش جزئیات، فرمول‌ها و محدودیت‌ها.
- `validation`: گزارش بررسی‌های انجام‌شده.

برای نصب معمولی، پوشه source را به‌صورت دستی روی کل پروژه کپی نکنید؛ نصب‌کننده فقط فایل‌های تغییرکرده را جایگزین می‌کند. فایل‌های .env واقعی، .git، داده‌ها و تصاویر اختصاصی فعلی از پروژه خودتان برداشته می‌شوند. node_modules و .next در نسخه جدید از نو ساخته می‌شوند. نسخه قبلی کامل در پوشه backup می‌ماند.

## پیش از اجرا

۱. از پنل پارس‌پک یک پشتیبان مستقل دیتابیس بگیرید؛ کپی پوشه پروژه پشتیبان دیتابیس راه‌دور نیست. این نصب هیچ migration، seed یا reset اجرا نمی‌کند.
۲. سرور محلی الوریا و فرایندهای watch را متوقف کنید؛ اگر با npm در ترمینال اجراست Ctrl+C بزنید. ویرایش فایل‌ها و تغییر تنظیمات را تا پایان نصب متوقف کنید. برنامه دیگری که در همین پوشه فایل می‌نویسد نیز باید متوقف باشد.
۳. Node و npm سازگار با پروژه فعلی، اینترنت برای npm و فضای کافی برای دو نسخه پروژه و وابستگی‌ها لازم است. از نسخه Node محیط فعلی استقرار استفاده کنید که با فایل قفل سازگار باشد.
۴. ZIP را در Downloads با نام ELORIA_FINAL.zip ذخیره کنید. پاورشل عادی کافی است؛ دسترسی Administrator لازم نیست مگر مجوز نوشتن پوشه C:\eloria محدود باشد.

## دستور نصب در PowerShell

```powershell
$Archive = Join-Path $env:USERPROFILE 'Downloads\ELORIA_FINAL.zip'
$Extract = Join-Path $env:USERPROFILE ('Downloads\ELORIA-Install-' + (Get-Date -Format 'yyyyMMdd-HHmmss'))
Expand-Archive -LiteralPath $Archive -DestinationPath $Extract
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$Extract\ELORIA_FINAL\INSTALL.ps1" -ProjectPath 'C:\eloria'
```

اگر پروژه جای دیگری است فقط مقدار ProjectPath را تغییر دهید. ExecutionPolicy فقط برای همین فرایند اعمال می‌شود و تنظیم سراسری ویندوز تغییر نمی‌کند.

نصب ابتدا اختلاف فایل‌ها را بررسی می‌کند. پیام `Local version differs` یعنی آن فایل نسبت به ZIP اولیه شما تغییر کرده؛ نصب متوقف شده و باید اختلاف ادغام شود. فایل را کورکورانه پاک یا جایگزین نکنید. مسیر فایل خطادار را برای بررسی بفرستید.

در خطای npm یا build، پروژه اصلی باقی می‌ماند و مسیر candidate و لاگ در کنار آن است. نصب ناقص را موفق تلقی نکنید. لاگ ممکن است اطلاعات محیط شما داشته باشد؛ قبل از ارسال، کلیدها و اطلاعات مشتری را حذف کنید. لازم نیست پوشه اصلی را حذف کنید.

## تنظیمات پرداخت و قیمت روی سرور

تنظیمات فعلی را حفظ و همین کلیدها را در پنل متغیرهای محیطی پارس‌پک یا فایل تنظیمات فعال بررسی کنید. فایل .env.example را جایگزین .env واقعی نکنید. وجود تنظیم متفاوت در .env.local یا پنل سرور ممکن است مقدار .env را بازنویسی کند.

```dotenv
NEXT_PUBLIC_SITE_URL=https://eloriagallery.ir
ELORIA_COMMERCE_ENABLED=true
ELORIA_PAYMENT_ENABLED=true
ELORIA_DYNAMIC_PRICING_ENABLED=true
```

همچنین `ZIBAL_MERCHANT` واقعی، `BRS_API_KEY`، `DATABASE_URL`، `DIRECT_URL`، `ELORIA_PAYMENT_START_SECRET` و کلیدهای امنیتی/رسید موجود پروژه باید معتبر باشند. کلیدها را از تنظیمات فعلی نگه دارید؛ کلید جدید را روی مقدار موجود ننویسید. اگر ELORIA_PAYMENT_START_SECRET تعریف نشده، در پوشه پروژه دستور زیر را اجرا و خروجی را فقط در همان تنظیمات خصوصی ذخیره کنید:

```powershell
node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
```

نشانی اختیاری قدیمی زرین‌پال را از متغیرهای مربوط به endpoint زیبال بردارید تا پیش‌فرض رسمی زیبال استفاده شود. callback این نسخه:

`https://eloriagallery.ir/api/payments/zibal/callback`

merchant باید فعال و دامنه با تنظیمات زیبال هماهنگ باشد. اتصال خروجی سرور به gateway.zibal.ir و سرویس بورس و دسترسی اینترنتی به callback لازم است. هیچ کلید واقعی در بسته قرار داده نشده است.

## پس از پیام SUCCESS

```powershell
Set-Location 'C:\eloria'
npm run check:env
```

این بررسی به تنظیمات production نیاز دارد؛ اگر خطا داد، همان متغیر را در محل تنظیمات واقعی اصلاح کنید. برای اجرای محلی اگر متغیرها در .env هستند:

```powershell
Set-Location 'C:\eloria'
node --env-file=.env scripts/start-production.mjs
```

اگر متغیرها را مدیر فرایند یا سیستم تنظیم می‌کند، دستور جاری `npm start` را استفاده کنید. دستور --env-file نیازمند نسخه Node پشتیبان آن است. فایل .env.local در این فرمان خودکار به متغیرهای فرایند والد اضافه نمی‌شود؛ در صورت استفاده، محل فایل فعال خودتان را مشخص کنید. برای آزمون OTP، کوکی امن و پرداخت، از محیط HTTPS با تنظیمات دامنه صحیح استفاده کنید؛ localhost تأیید پرداخت سایت اصلی نیست.

نصب در C:\eloria سایت پارس‌پک را خودکار تغییر نمی‌دهد. پس از موفقیت ساخت محلی، فایل‌های اصلاح‌شده را با روش انتشار فعلی خودتان به سرور منتقل کنید. node_modules و .next ساخته‌شده ویندوز را به سرور لینوکسی منتقل نکنید؛ روی سرور با تنظیمات فعلی این دستورات را اجرا کنید و فقط پس از موفقیت، سرویس فعلی را از پنل میزبانی بازراه‌اندازی کنید:

```text
npm ci
npx prisma generate
npm run build
```

اگر انتشار از GitHub است، تغییرات را در مخزن خودتان بررسی و طبق روند انتشار فعلی commit و push کنید. نام شاخه یا فرمان سرویس پارس‌پک از این گفتگو معلوم نیست و در نصب‌کننده حدس زده نشده است. در حالت ارسال فایل، فهرست دقیق فایل‌ها در update-manifest.json است. تنظیمات پنل سرور را حفظ کنید.

پس از تنظیم کلید بورس و پیش از فعال‌کردن فروش نقره پویا، در محیط سرور:

```text
npm run metal:sync
```

این دستور نرخ‌های دیتابیس را به‌روزرسانی می‌کند؛ فقط پس از نصب موفق و با کلید صحیح اجرا کنید. زمان‌بندی به‌روزرسانی موجود را فعال نگه دارید.

یک محصول طلا و نقره در هر دو حالت دستی و پویا، سبد چندتایی، مبلغ سفارش و مبلغ درگاه، پرداخت موفق/لغو و بازگشت تکراری را بررسی کنید. مبلغ درگاه ریال است و قیمت فروشگاه تومان؛ باید دقیقاً ده برابر باشد. بسته‌بندی ۷۰ هزار تومان برای هر قطعه و ارسال ۱۷۰ هزار تومان برای کل سفارش است. در قیمت دستی بسته‌بندی و ارسال را دوباره وارد نکنید.

طلا طبق درخواست: اصل فلز = وزن × نرخ؛ اجرت ۸٪ اصل فلز؛ سود ۷٪ اصل فلز؛ ۹٪ مجموع سود و اجرت؛ به‌اضافه کار هنری و هزینه‌های ثابت. نقره طبق ضریب تجاری درخواستی ۱۰٫۳۱ است؛ این ضریب تبدیل استاندارد اونس به گرم نیست. این بسته نرخ مالیاتی قانونی روز را تأیید نمی‌کند. جزئیات در گزارش آمده است.

## بازگشت به نسخه قبل در ویندوز

سرور محلی را متوقف کنید. در دستور زیر مسیر backup را دقیقاً از خروجی نصب بردارید. دیتابیس را این فرمان‌ها تغییر نمی‌دهند:

```powershell
Set-Location 'C:\'
$Backup = 'C:\eloria-backup-REPLACE_WITH_ACTUAL_TIMESTAMP'
if (-not (Test-Path -LiteralPath "$Backup\package.json")) { throw 'Backup path is incorrect' }
$Failed = 'C:\eloria-reverted-' + (Get-Date -Format 'yyyyMMdd-HHmmss')
Move-Item -LiteralPath 'C:\eloria' -Destination $Failed
try { Move-Item -LiteralPath $Backup -Destination 'C:\eloria' }
catch { Move-Item -LiteralPath $Failed -Destination 'C:\eloria'; throw }
```

این بازگشت مخصوص رایانه محلی است. برای پارس‌پک از نسخه انتشار قبلی همان سرور استفاده کنید. اگر پس از نصب فایل یا تصویر تازه ایجاد شده، قبل از بازگشت آن را جدا نگه دارید. پوشه backup حاوی تنظیمات خصوصی است و نباید در GitHub یا مسیر عمومی وب آپلود شود.
