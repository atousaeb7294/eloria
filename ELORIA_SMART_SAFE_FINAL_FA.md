# ELORIA Smart Safe Final — 2026-08-27

این نسخه بر پایه‌ی **ELORIA_FINAL_PRODUCTION_2026-08-27 + Intro Fixed v8** ساخته شده و هدف آن تکمیل بازاریابی، سئو، تجربه کاربری و هوشمندی فروشگاه بدون بازطراحی پرریسک دیتابیس است.

## تغییرات فعال

- هات‌فیکس Intro v8 حفظ شده: پرده اول صریحاً با ورود به فاز خود شروع می‌شود؛ پرده دوم فقط بعد از کلیک کاربر و ورود به Home فقط بعد از پایان واقعی پرده دوم است.
- کد عمومی خرید اول `ELORIA50` است و مزیت آن **مبلغ ثابت ۵۰٬۰۰۰ تومان** است؛ ورودی تاریخی `ELORIA10` فقط برای سازگاری به همین کد نگاشت می‌شود.
- Migration داده برای نرمال‌سازی رکورد قدیمی به `ELORIA50` با `FIXED_TOMAN = 50000` اضافه شده است.
- صفحه محصول نرخ/درصد اجرت، سود یا مالیات را عمومی نمی‌کند. فقط قیمت نهایی، وزن و نرخ لحظه‌ای فلز نمایش داده می‌شود.
- زیر قیمت، توضیح لوکس و کوتاه درباره‌ی اینکه قیمت نهایی شامل ارزش روز فلز، اجرت ساخت، سهم سود و ارزش هنری قطعه‌ی دست‌بافت الوریاست نمایش داده می‌شود.
- پیشنهاد محصول مرتبط در صفحه محصول با قیمت زنده و موجودی واقعی افزوده شده است.
- اشتراک‌گذاری محصول و کپی لینک با ثبت رویداد رضایتی `share_product` افزوده شده است.
- جستجوی فارسی نرمال شده است: ی/ک عربی، نیم‌فاصله، اعداد فارسی و عربی و جستجو در توضیح/افسانه محصول.
- عبارت‌های ساده‌ی هوشمند مثل «دستبند طلا زیر ۳۰ میلیون» به فیلتر واقعی دسته، جنس و بودجه تبدیل می‌شوند.
- صفحات فیلترشده کاتالوگ `noindex,follow` و Canonical به صفحه اصلی محصولات دارند تا URLهای فیلتر، سئوی تکراری نسازند.
- Product Structured Data با جنس، وزن، عیار، موجودی، قیمت نهایی و ارسال رایگان تکمیل شده است؛ اطلاعات مالی داخلی افشا نمی‌شود.
- پنل Intelligence فرصت «بازدید زیاد / فروش صفر» را از داده واقعی ۳۰ روزه تشخیص می‌دهد.
- نرخ‌های تقریبی Funnel (محصول→سبد، سبد→پرداخت، پرداخت→خرید) در پنل Intelligence نمایش داده می‌شود.
- RFM زنده‌ی مشتریان از سفارش‌های موجود محاسبه می‌شود و مشتریان را به VIP، وفادار، جدید، فعال و در معرض ریزش تقسیم می‌کند؛ بدون جدول یا Migration جدید.
- Quality Score صفحه محصول از توضیح، داستان اثر، وزن/عیار، تصویر و ALT محاسبه می‌شود و محصولات نیازمند بهبود در پنل مشخص می‌شوند.
- انتخاب هوشمند روزانه Home، PWA، Timeline محصول، فرم تماس، شبکه‌های اجتماعی، راهنمای مچ، ارسال رایگان و داستان «سرزمین کهن» نسخه پایه حفظ شده‌اند.

## عمداً در این بسته Production وارد نشده

قابلیت‌های زیر ارزشمندند، اما برای اضافه‌شدن صحیح به فروشگاه واقعی نیاز به طراحی Migration و تبدیل داده موجود دارند؛ بنابراین برای جلوگیری از خراب‌شدن دیتابیس فعلی کورکورانه اضافه نشده‌اند:

- موجودی تک‌قطعه‌ای/InventoryPiece برای هر قطعه طلا
- گواهی اصالت و QR دائمی هر قطعه
- Wallet / Loyalty Ledger مالی
- مدل ذخیره‌شونده‌ی RFM (نسخه فعلی RFM را زنده محاسبه می‌کند)
- Attribution دائمی UTM در Order
- سیستم مستقل خدمات پس از فروش/تعمیر/تغییر سایز

این موارد باید در یک فاز Migration جدا با Backup دیتابیس اجرا شوند.

## پس از جایگزینی روی C:\\eloria

نیازی به `db:seed` نیست و روی دیتابیس واقعی **Seed اجرا نکنید**.

دستورهای لازم:

```powershell
Set-Location C:\eloria
npx prisma generate
npx prisma migrate status
npx prisma migrate deploy
npm run dev
```

Migration جدید فقط رکورد کوپن خرید اول را از درصدی به مبلغ ثابت ۵۰٬۰۰۰ تومان تبدیل می‌کند.
