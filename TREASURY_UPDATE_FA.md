# نسخهٔ صحنه‌های گنجینه و اصلاح خرید و تصاویر

این راهنما مربوط به همین بسته است و توضیحات حرکتی نسخه‌های قبلی را جایگزین می‌کند.

## نصب در C:\eloria

ZIP را بیرون از پروژه استخراج کنید. PowerShell را در پوشهٔ eloria-main استخراج‌شده باز کنید و اجرا کنید:

```powershell
powershell -ExecutionPolicy Bypass -File .\INSTALL_STORY_UPDATE.ps1 -Destination C:\eloria
```

برنامهٔ در حال اجرای npm run dev را قبل از نصب با Ctrl+C متوقف کنید. اسکریپت فایل‌های هدف را با نسخه‌های شناخته‌شده مقایسه می‌کند، از آن‌ها پشتیبان می‌گیرد و در صورت پیدا کردن تغییر دستی متوقف می‌شود. .env و .git وارد نصب نمی‌شوند. پس از نصب و بررسی موفق، در C:\eloria فرمان npm run dev را اجرا کنید.

برای این تغییرات migration، seed و دیتابیس محلی لازم نیست. DATABASE_URL و DIRECT_URL موجود خودتان برای پارس‌پک حفظ می‌شوند. نصب و ساخت به معنی انتشار روی سایت یا push به GitHub نیست.

## خانه

بنر و سه گنجینه داخل یک قاب تمام‌قد نمایش داده می‌شوند. حرکت موس در این محدوده یک فصل را کامل می‌کند. تکانه‌های ادامه‌دار ترک‌پد فصل‌ها را رد نمی‌کنند و جهت معکوس قابل استفاده است. حرکت طلا از پایین، نقره با چرخش جانبی و بافت از عمق انجام می‌شود. در پایان، کنترل به اسکرول عادی فوتر برمی‌گردد.

اثر حرکت با transform و opacity اجرا می‌شود؛ فقط دو فصل در حال جابه‌جایی رسم می‌شوند. حلقهٔ انیمیشن در سکون متوقف است. blur و clip-path متحرک تمام‌صفحه نداریم. در نمایشگر خیلی کوتاه یا تنظیم «کاهش حرکت»، محتوا با ترتیب عادی نمایش داده می‌شود.

## بندانگشتی محصولات

پس از ثبت محصول، وضعیت آن باید ACTIVE یا OUT_OF_STOCK باشد، دسته‌بندی باید فعال باشد و تصویر واقعی داشته باشد. گنجینهٔ طلا از hasGold و نقره از hasSilver استفاده می‌کند. اثر دارای هر دو فلز در هر دو گنجینه دیده می‌شود. بافت، محصول بدون هر دو فلز است. هر گنجینه حداکثر پنج بندانگشتی در خانه دارد. درخواست با بازگشت به تب و به‌صورت دوره‌ای تازه می‌شود؛ به‌دلیل cache کوتاه، انتشار عکس ممکن است بلافاصله در تب باز دیده نشود.

تصاویر بزرگ معرفی گنجینه، تصویرسازی معرفی‌اند؛ بندانگشتی‌ها عکس محصولات واقعی هستند. اگر محصول قابل نمایش وجود نداشته باشد، محصول ساختگی نشان داده نمی‌شود.

## کارت‌های گنجینه

کارت‌ها کوچک و قاب‌دار، در یک ردیف افقی هستند. نام فارسی فونت خطاطی سایت دارد. زیر آن قیمت، خرید سریع و «دیدن صفحهٔ اثر» قرار دارد. اشارهٔ موس کارت را برجسته می‌کند؛ کلیک روی کارت کناری آن را انتخاب می‌کند. کلیک روی عکسِ کارت انتخاب‌شده یا لینک صفحهٔ اثر، مشخصات را باز می‌کند. روی موبایل کشیدن افقی و دکمه‌های انتخاب در دسترس‌اند.

هر حرکت موس در محدودهٔ نمایش آثار یک کارت را جلو می‌آورد. بیرون این محدوده، فیلترها و صفحه اسکرول عادی دارند. آثار بعدی در بسته‌های ۲۴تایی دریافت و به همین ردیف اضافه می‌شوند. برای خطای شبکه، دکمهٔ دریافت دوباره وجود دارد. خرید سریع به همان قیمت‌گذاری و سبد خرید واقعی سایت متصل است.

## قیمت

نرخ طلای خام از /api/metal-prices، یعنی نرخ ثبت‌شدهٔ منبع بازار، خوانده می‌شود. عیار و زمان منبع نمایش داده می‌شوند؛ نرخ قدیمی با عنوان آخرین نرخ مشخص می‌شود. تازه‌شدن واقعی نرخ همچنان به cron فعلی دریافت قیمت بستگی دارد.

اجرت و سود در ریز محاسبه به درصد نمایش داده می‌شوند؛ برای طلای فعلی، ۸٪ اجرت و ۷٪ سود بر ارزش طلا. در اثر ترکیبی، این درصدها مربوط به بخش طلا هستند. ردیف بسته‌بندی از نمایش حذف شده است؛ درخواست کاربر تغییر نمایش بود و فرمول و مبلغ نهایی حفظ شده‌اند.

## آپلود عکس ادمین

اشکال قطعی قبلی: فرم ۸ عکسِ ۸ مگابایتی را یک‌جا می‌فرستاد اما سقف Server Action مجموعاً ۱۰ مگابایت بود. آپلود جدید هر عکس را در یک درخواست مستقل می‌فرستد. نام، پیش‌نمایش و نتیجهٔ هر عکس مشخص است. اعتبارسنجی نشست ادمین، حجم و محتوای واقعی تصویر سمت سرور باقی مانده‌اند.

فضای تصویر پروژهٔ فعلی Supabase Storage است؛ اتصال دیتابیس پارس‌پک به‌تنهایی فضای آپلود عکس ایجاد نمی‌کند. سه تنظیم زیر باید مقادیر واقعی داشته باشند (مقدار آن‌ها را در چت نفرستید):

- SUPABASE_URL
- SUPABASE_SERVICE_ROLE_KEY
- ELORIA_STORAGE_BUCKET

نام bucket باید درست و عکس‌های آن برای نمایش عمومی قابل خواندن باشند. با تغییر تنظیمات محیطی، برنامه را دوباره راه‌اندازی/ساخت کنید. روی سرور واسط هم محدودیت بدنه باید یک فایل ۸ مگابایتی به‌علاوهٔ سربار فرم را بپذیرد.

بررسی فقط‌خواندنی تنظیمات و دسترسی:

```powershell
cd C:\eloria
node --import tsx scripts/check-media-storage.ts
```

این فرمان هیچ عکس یا رکوردی ایجاد یا حذف نمی‌کند و مقدار کلیدها را چاپ نمی‌کند.

## اعتبارسنجی

جزئیات اجرا در VALIDATION_STORY.txt است. بررسی TypeScript، lint و build با متغیرهای آزمایشی انجام شده‌اند. دیتابیس پارس‌پک، کلید واقعی فضای تصاویر، ورود واقعی ادمین و پرداخت در دسترس این محیط نبودند.

مرورگر ابری آدرس محلی را مسدود کرد؛ آزمون دیداری دسکتاپ و موبایل در این محیط تأیید نشده است. تست مستقل حرکت و عضویت بندانگشتی‌ها برای اجرای روی سیستم شما آماده است:

```powershell
npx playwright test --config=playwright.treasury.config.ts
```

این تست با Chrome نصب‌شده اجرا می‌شود؛ داده‌های نمونه فقط پاسخ شبکهٔ همان مرورگر آزمون را جایگزین می‌کنند و در دیتابیس ذخیره نمی‌شوند. اسکرین‌شات‌ها در test-results قرار می‌گیرند. این تست خرید یا بارگذاری واقعی انجام نمی‌دهد.
