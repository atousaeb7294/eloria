# به‌روزرسانی گنجینه‌های الوریا

این نسخه روی پروژهٔ ارسالی ساخته شده است. فایل واقعی .env، دیتابیس پارس‌پک و مخزن GitHub شما در این محیط در دسترس نبوده و تغییر نکرده‌اند. طرح جدید از همان مسیرهای واقعی محصولات، قیمت‌گذاری و سبد خرید استفاده می‌کند. هیچ migration یا seed جدیدی لازم نیست.

## نصب در C:\eloria

فایل ZIP را در پوشه‌ای جدا از پروژه باز کنید. از PowerShell داخل پوشهٔ استخراج‌شده اجرا کنید:

```powershell
powershell -ExecutionPolicy Bypass -File .\APPLY_TREASURY_UPDATE.ps1 -Destination C:\eloria
cd C:\eloria
npm ci
npx prisma generate
npm run typecheck
npm run lint
npm run build
npm run dev
```

اسکریپت فقط فایل‌های تغییرکردهٔ همین نسخه را اعمال می‌کند، از نسخهٔ قبلی آن‌ها پشتیبان می‌گیرد و .env و .git را دست نمی‌زند. اگر یکی از فایل‌های هدف نسبت به ZIP اولیه تغییر کرده باشد، قبل از هر تغییری متوقف می‌شود تا تغییرات جدید شما پاک نشود. برای اجرای واقعی، همان .env کامل خودتان را نگه دارید: DATABASE_URL و DIRECT_URL باید به دیتابیس پارس‌پک اشاره کنند. دیتابیس محلی لازم نیست. هیچ فرمان migrate reset یا db seed برای این به‌روزرسانی اجرا نکنید.

## تجربهٔ جدید

خانه: بنر فعلی ← تاب مهر (طلا) ← نور ماه (نقره) ← تار جان (بافت) ← فوتر. نمایش بی‌نهایت قدیمی حذف شده است. در دسکتاپ یک حرکت چرخ موس به بخش بعدی می‌رود؛ حرکت با easing پیوسته انجام می‌شود. موبایل از اسکرول طبیعی و لایه‌های sticky استفاده می‌کند. در نمایشگر کوتاه و حالت کاهش حرکت، خوانایی و دسترسی بر افکت اولویت دارند.

کلیک روی صحنه گنجینه را باز می‌کند؛ آیکون‌های کوچک، عکس محصولات واقعی همان گنجینه هستند. اگر دیتابیس در دسترس نباشد، تصاویر اختصاصی و لینک گنجینه‌ها باقی می‌مانند و محصول ساختگی نمایش داده نمی‌شود. دریافت پیش‌نمایش حداکثر ۶ اثر از هر گنجینه است.

داخل گنجینه: نمایش بزرگ آثار، حرکت افقی لمسی، کنترل قبلی/بعدی، بندانگشتی‌های انتخاب و خرید سریع در پنجرهٔ همان صفحه. لینک محصول به مشخصات واقعی آن می‌رود. انتقال‌های چپ و بالا با View Transitions انجام می‌شوند؛ مرورگر فاقد این قابلیت یا حالت reduced motion، مسیریابی معمولی دارد.

سه تصویر جدید، تصویرسازی معرفی گنجینه‌اند و جای عکس واقعی محصول را نمی‌گیرند. فایل‌ها: public/images/treasuries/gold.webp، silver.webp و weave.webp. مجموع حجم حدود ۴۸۴ کیلوبایت.

## نتیجهٔ بررسی قیمت

تعریف «حاشیهٔ سود» مطابق توضیح آخر شما: فقط افزایش اضافی بازار بسته حذف است؛ سود معمول طلافروشی حفظ می‌شود. کد اولیه نیز این افزایش را صفر کرده بود. توضیح فیلد قدیمی اصلاح و تست عدم اثرگذاری آن اضافه شد؛ نیازی به دستکاری دیتابیس نیست.

- طلا: ارزش وزن و عیار طلا + اجرت ۸٪ ارزش طلا + سود ۷٪ اصل طلا + هنر دست + ۷۰٬۰۰۰ تومان بسته‌بندی هر قطعه. مالیات صفر.
- نقره: نرخ موجود با مبنای اختصاصی فعلی پروژه (اونس دلاری ÷ ۱۰٫۳۱ × دلار) × وزن + هنر دست + بسته‌بندی. فرمول نقره تغییر نکرده است.
- ترکیبی: اجزای طلا و نقره جدا محاسبه می‌شوند؛ هنر دست و بسته‌بندی فقط یک‌بار برای هر محصول اضافه می‌شوند. در رکوردهای دارای فلز اصلی نقره، عیار نقره دیگر به‌اشتباه به جزء طلا اعمال نمی‌شود؛ مبنای طلای چنین رکوردهایی ۷۵۰ است. برای طلای دارای عیار دیگر، فلز اصلی را طلا و عیار آن را صحیح ثبت کنید.
- ارسال ۱۷۰٬۰۰۰ تومان فقط یک‌بار برای کل سفارش، در سبد خرید اضافه می‌شود.
- باکس خرید سریع و مشخصات، قیمت و ریز محاسبه را از یک پاسخ سرور می‌گیرند. هر ۳۰ ثانیه و با برگشت به پنجره بررسی می‌شود. قطع نرخ معتبر خرید جدید را غیرفعال می‌کند.
- تسویه قبل از ثبت سفارش دوباره قیمت می‌گیرد و در صورت تغییر، تأیید دوبارهٔ مشتری لازم است. پس از ایجاد درخواست درگاه، مبلغ درخواست ثابت می‌ماند؛ تغییر مبلغ در حین پرداخت انجام نشده است.

نمونهٔ تست: ۲ گرم طلا با نرخ ۱۰٬۰۰۰٬۰۰۰ + ۳٫۲۵ گرم نقره با نرخ ۱۰۰٬۰۰۰ + هنر دست ۵۰۰٬۰۰۰ → قیمت محصول ترکیبی ۲۳٬۸۹۵٬۰۰۰ تومان، شامل بسته‌بندی. هزینهٔ ارسال سفارش جدا است.

## پنل ادمین

در بخش «ترکیب فلز و عضویت در گنجینه‌ها» هر دو تیک مستقل‌اند. برای اثر ترکیبی وزن مثبت هر دو فلز اجباری شده است و سمت سرور هم بررسی می‌شود. برای گنجینهٔ بافت، هر دو تیک خاموش و قیمت‌گذاری ثابت انتخاب می‌شود؛ خطایی که قبلاً ذخیرهٔ بافت بدون فلز را مسدود می‌کرد اصلاح شد.

## آزمون‌ها و محدودیت‌ها

آزمون‌های محاسبهٔ طلا/نقره/ترکیبی، ارسال، مالیات صفر و بازار بسته بدون افزایش اجرا شدند. آزمون جدید scripts/test-treasury-pricing.ts برابری قیمت کاتالوگ و محصول ترکیبی را بررسی می‌کند. نتایج نهایی ساخت و TypeScript در VALIDATION_TREASURY.txt درج شده‌اند.

مرورگر این محیط آدرس محلی را باز نکرد؛ بررسی دیداری و تعامل واقعی دسکتاپ و موبایل تأیید نشده است. محتوای تعاملی صفحهٔ مرجع نیز در مرورگر خالی ماند؛ ادعای تطابق پیکسل‌به‌پیکسل با مرجع نداریم. تست اتصال دیتابیس پارس‌پک، ذخیرهٔ واقعی محصول در ادمین و پرداخت واقعی نیازمند محیط خود شماست. بهینه‌بودن مطلق سرعت و امنیت کل پروژه ادعا نشده است.

بعد از نصب، مسیر خانه تا فوتر، حرکت معکوس، بندانگشتی، خرید سریع، انتخاب مدل، بازگشت مرورگر و یک سفارش آزمایشی در محیط تست خودتان بررسی شود. سپس تغییرات را از مخزن موجودتان commit و push کنید.

## تکمیل تجربهٔ حرکت و فیلتر (۲۳ شهریور ۱۴۰۵)

سه گنجینه با سه ورود عمقی متفاوت ارائه می‌شوند: طلا از پایین با چرخش افقی، نقره از کنار با چرخش عمودی و بافت با چرخش و عمق بیشتر. اسکرول پشت‌سرهم در صحنه‌ها حرکت را از موقعیت جاری به فصل بعد یا قبل می‌برد. فیلترهای تمام آثار و هر گنجینه همواره باز هستند؛ با انتخاب جنس، نوع اثر، موجودی یا بازهٔ قیمت، نتیجه خودکار به‌روز می‌شود. جست‌وجوی متنی پس از مکث کوتاه اعمال می‌شود؛ Enter نیز نتیجه را فوری ثبت می‌کند. تغییر ناخواستهٔ نوع اثر با حرکت موس برداشته شد.

این نسخه همان اسکریپت نصب محافظه‌کار را دارد و هش فایل‌های تغییرکرده در manifest به‌روز شده است. نسخهٔ قبلی آپدیت نیز برای دو فایل اصلاح‌شده به‌عنوان مبدأ قابل قبول ثبت شده است. اگر فایل‌ها را پس از نصب دستی تغییر داده‌اید، اسکریپت برای جلوگیری از بازنویسی آن تغییرات متوقف می‌شود.

## اصلاح فوری عملکرد صفحهٔ خانه (۲۳ شهریور ۱۴۰۵)

حرکت اجباری چرخ موس و پردازش هم‌زمان blur، clip-path و تبدیل سه‌بعدی تمام فصل‌ها حذف شد. گنجینه‌ها با اسکرول طبیعی به ترتیب روی فصل قبلی می‌آیند و تصویر هر گنجینه زاویهٔ ثابت و سبک متفاوتی دارد. افکت ذرات و حرکت اشاره‌گر صفحهٔ خانه نیز کنار گذاشته شد. فیلترهای باز و اعمال خودکار از نسخهٔ پیشین باقی‌اند. برای جلوگیری از بازنویسی تغییرات دستی، نصب‌کننده قبل از تغییر فایل‌ها هش را بررسی و نسخهٔ پشتیبان تهیه می‌کند.

این اصلاح به‌دلیل گزارش هنگی کاربر انجام شد. تا اجرای پروژه روی مرورگر و دستگاه واقعی، روانی نهایی ادعا نمی‌شود.
