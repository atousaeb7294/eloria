# ELORIA — Final Smart Production R3

این نسخه بر پایه نسخه Production مورخ 2026-08-27 و Intro Fixed v8 ساخته شده و برای ارتقای مستقیم پروژه فعلی طراحی شده است.

## تجربه محصول
- کارت‌های «قیمت‌گذاری شفاف» و «موجودی واقعی» از Product Page حذف شده‌اند.
- «مشخصات محصول» بلافاصله بعد از افزودن به منتخب‌ها قرار دارد؛ سپس ارسال مهمان الوریا، اتصال تا پرداخت و مشخصات دقیق و بعد «درباره این محصول» می‌آید.
- مبلغ جداگانه اجرت، سود یا مالیات در صفحه محصول افشا نمی‌شود؛ فقط قیمت نهایی، وزن و نرخ لحظه‌ای فلز باقی می‌ماند.
- توضیح ظریف زیر قیمت بیان می‌کند که مبلغ نهایی شامل ارزش روز فلز، اجرت، سهم سود و ارزش هنری قطعه دست‌بافت است.
- متن دستبندها اعلام می‌کند سایز استاندارد است و شخصی‌سازی با درج اندازه مچ در توضیحات سفارش انجام می‌شود.

## Checkout و تخفیف
- توضیحات سفارش/شخصی‌سازی از UI تا دیتابیس و پنل سفارش مدیر متصل است.
- کد عمومی `ELORIA50` مبلغ ثابت ۵۰٬۰۰۰ تومان دارد.
- Preview تخفیف با قیمت و موجودی دوباره‌محاسبه‌شده سرور انجام می‌شود.
- ثبت نهایی دوباره کد را با قفل تراکنشی بررسی می‌کند؛ فقط خرید اول و یک بار برای هر شماره موبایل.
- جمع اقلام، ارسال، تخفیف و مبلغ قابل پرداخت از Quote سرور نمایش داده می‌شود.

## هوشمندی فروش و تحلیل رفتار
- انتخاب روزانه Home از تازگی، بازدید، فروش، منتخب‌ها، Featured و موجودی امتیاز می‌گیرد.
- Related Products با نزدیکی قیمت، دسته و رفتار ۳۰ روزه رتبه‌بندی می‌شوند.
- جستجوی فارسی، اعداد فارسی/عربی و عبارت‌های بودجه پشتیبانی می‌شوند.
- Event Tracking رضایتی برای Funnel، جستجو، فیلتر، منتخب و کد تخفیف وجود دارد.
- RFM مشتری، فرصت‌های بازدید بالا/فروش پایین، ریزش Checkout و Product Quality در Intelligence Center محاسبه می‌شوند.
- UTM منبع/رسانه/کمپین تا سفارش حفظ و فروش هر منبع در ادمین گزارش می‌شود.
- تولید افسانه محصول پیش‌فرض رایگان و deterministic است؛ Ollama محلی اختیاری است. OpenAI فقط با انتخاب صریح مدیر فعال می‌شود.

## SEO و امنیت
- Canonical/Sitemap/Product Schema/PWA قبلی حفظ شده‌اند.
- Organization Schema شبکه‌های اجتماعی رسمی تنظیم‌شده را با `sameAs` منتشر می‌کند.
- فیلترهای کاتالوگ از ایندکس تکراری محافظت می‌شوند.
- API تولید افسانه فقط با Session معتبر ادمین در دسترس است.
- مبلغ خرید، تخفیف، موجودی و قیمت در سرور مجدداً اعتبارسنجی می‌شوند.
- هیچ Migration مخرب برای موجودی تک‌قطعه‌ای، Wallet یا Certificate در این Release اعمال نشده است.

برای ارتقا از `INSTALL_ELORIA_FINAL.ps1` استفاده کنید. `db:seed` در ارتقا اجرا نشود.
