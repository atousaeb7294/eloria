# ELORIA Smart Production R4 — انتشار نهایی

این بسته بر پایه **ELORIA Final Smart Production R3** ساخته شده و همه قابلیت‌های R3، Intro پایدار، تخفیف خرید اول ۵۰٬۰۰۰ تومانی، هوشمندی فروش، SEO و امنیت قبلی را حفظ می‌کند.

## تغییرات R4

- Intro دارای دکمه «رد کردن» است و کاربر می‌تواند بدون تماشای دو پرده وارد سایت شود.
- دکمه «ورود به دنیای الوریا» که داخل فریم‌های پایانی ویدئوی اول دیده می‌شود، بدون ساخت دکمه نمایشی دوم توسط سایت قابل کلیک شده است. یک hotspot شفاف فقط هنگام دیده‌شدن همان دکمه فعال می‌شود.
- کلید نشست Intro به v10 ارتقا یافته تا نسخه جدید در اولین اجرای پس از نصب به‌درستی تست شود.
- تعداد انتخاب‌های هوشمند Home تا ۱۲ اثر افزایش یافته و یک چیدمان تصویری زنده با اندازه‌های متنوع، حرکت بسیار ملایم و Lazy Loading اضافه شده است.
- CTA اصلی Home از «تماشای آثار» به «جست‌وجو در تمام آثار» تغییر یافته است.
- لوگوی هدر با کنتراست، روشنایی و Glow کنترل‌شده خواناتر شده است.
- صفحات واقعی `cart` و `checkout` به نسخه ماژولار جدید متصل شده‌اند؛ بنابراین کادر توضیحات سفارش و تخفیف دیگر فایل بلااستفاده نیستند و در مسیر واقعی سایت دیده می‌شوند.
- کادر «توضیحات و شخصی‌سازی سفارش» به سبد خرید اضافه شده و متن آن از Cart به Checkout و سپس Order/Database منتقل می‌شود.
- کادر تخفیف Checkout حفظ و برجسته است و `ELORIA50` را به‌صورت سروری اعتبارسنجی می‌کند: مبلغ ثابت ۵۰٬۰۰۰ تومان، فقط خرید اول و فقط یک بار برای هر شماره موبایل.
- دکمه تماس در هدر و منوی موبایل مستقیماً به صفحه `/contact` می‌رود.
- فرم تماس با `eloria-panel` و `eloria-field` کادربندی و Focus State منسجم دارد و همچنان به API واقعی پشتیبانی متصل است.
- Navigation Accelerator برای Prefetch مسیرهای پرتکرار و Prefetch لینک داخلی هنگام Hover، Focus و Pointer Down اضافه شده است تا حس پاسخ‌گویی کلیک‌ها سریع‌تر شود.
- زمان Transition عمومی عناصر تعاملی کوتاه‌تر شده و `touch-action: manipulation` برای واکنش بهتر موبایل اضافه شده است.
- درخواست Home Featured به cache مرورگر اجازه استفاده از Cache-Control سرور را می‌دهد تا درخواست‌های تکراری سریع‌تر شوند.

## نکات حفظ‌شده از نسخه قبلی

- اجرت، سود و مالیات به‌صورت عدد جداگانه در صفحه محصول نمایش داده نمی‌شوند.
- فقط قیمت نهایی، وزن و نرخ لحظه‌ای فلز در رابط اصلی دیده می‌شوند و توضیح ترکیب قیمت به‌صورت اطلاع‌رسانی ظریف باقی مانده است.
- سایز تمام دستبندها استاندارد معرفی می‌شود و شخصی‌سازی از طریق توضیحات سفارش انجام می‌شود.
- `db:seed` در Installer اجرا نمی‌شود.
- `.env` فعلی پروژه هنگام ارتقا حفظ می‌شود.
- Backup کامل پروژه قبل از جایگزینی ساخته می‌شود.

## کنترل انتشار

`scripts/verify-final-release.mjs` شامل کنترل‌های اختصاصی R4 است. در زمان بسته‌بندی این Release، کنترل‌های Release اختصاصی R4 به‌روزرسانی شده‌اند و AST همه فایل‌های TypeScript/TSX بدون Syntax/JSX Error بررسی شده است.

بعد از نصب روی سیستم مقصد، برای تأیید Production اجرا شود:

```powershell
npm run typecheck
npm run lint
npm run build
```
