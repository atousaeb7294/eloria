# گزارش رفع ایرادهای بحرانی ELORIA

تاریخ بررسی: ۱۴۰۵/۰۶/۰۹ (2026-08-31)

## نتیجه بررسی نسخه زنده

- صفحه اصلی و فهرست ۶ محصول در `https://eloriagallery.ir/fa/products` باز می‌شوند.
- صفحه جزئیات محصول در نسخه زنده به خطای Server Component می‌رسد.
- منوی «دنیای الوریا» با فوکوس صفحه‌کلید و بدون کلیک باز می‌شود.
- فرم‌های ورود مشتری و مدیر تا تکمیل Turnstile قفل هستند؛ در تلاش مجدد مدیر نیز توکن یک‌بارمصرف قبلی بازنشانی نمی‌شد.
- قیمت‌های فهرست در HTML اولیه وجود دارند، اما کامپوننت کارت وقتی قیمت اولیه داشت هیچ درخواست تازه‌ای نمی‌فرستاد؛ بنابراین قیمت نمایش‌داده‌شده واقعاً لحظه‌ای نبود.

## اصلاحات انجام‌شده

### صفحه محصول

- خواندن فیلدهای رسانه‌ای جدید افسانه با یک مسیر سازگار با دیتابیس قدیمی انجام می‌شود؛ نبودن ستون‌های جدید دیگر کل صفحه محصول را از کار نمی‌اندازد.
- مهاجرت‌های قدیمیِ ستون‌های افسانه idempotent شدند تا نصب تازه با خطای ستون تکراری متوقف نشود.
- خطای منبع قیمت دیگر باعث سقوط کل صفحه نمی‌شود؛ مشخصات و تصاویر محصول نمایش داده می‌شوند و خرید تا بازیابی نرخ معتبر غیرفعال می‌ماند.
- نرخ قدیمی/غیرقابل‌فروش فقط اطلاع‌رسانی می‌شود و دکمه خرید را فعال نمی‌کند.

### قیمت لحظه‌ای

- کارت محصول حتی با داشتن قیمت اولیه، هنگام mount و سپس هر ۶۰ ثانیه در تب فعال قیمت تازه را با `cache: no-store` دریافت می‌کند.
- پاسخ API قیمت نیز `private, no-store` شد.
- قالب محیط ParsPack با تنظیمات ضروری BRS، همگام‌سازی داخلی، حدود کنترل جهش، Storage، Proxy و فاصله به‌روزرسانی کامل شد.

### ورود مشتری و مدیر

- دامنه Turnstile اکنون هر دو hostname دقیق تعریف‌شده در `NEXT_PUBLIC_SITE_URL` و `ELORIA_ALLOWED_ORIGINS` را می‌پذیرد؛ هیچ wildcard پذیرفته نمی‌شود.
- تلاش ناموفق مدیر، Turnstile مصرف‌شده را حذف و چالش تازه ایجاد می‌کند.
- حالت غیرفعال مجاز Turnstile در محیط غیرتولیدی دیگر فرم را برای همیشه قفل نمی‌کند.
- کانال‌های ایمیل و پیامک مشتری اکنون از تنظیمات واقعی محیط خوانده می‌شوند. اگر Resend آماده نباشد ولی Kavenegar فعال باشد، ورود پیامکی واقعاً قابل انتخاب است.
- API سرور درخواست کانالی را که پیکربندی نشده باشد رد می‌کند؛ UI و backend دیگر با هم ناسازگار نیستند.

### منو

- بازشدن با hover و focus حذف شد.
- منو فقط با دکمه باز/بسته می‌شود و با Escape یا کلیک بیرون بسته می‌شود.
- وضعیت `aria-expanded` روی کنترل واقعی منو قرار گرفت.

## تست‌های انجام‌شده

- `npm run typecheck` — موفق
- `npm run lint` — موفق
- `npm run test:critical` — موفق
- `npm run test:pricing` — تمام تست‌های موتور قیمت، تازگی نرخ، بازار بسته، timestamp و کنترل جهش موفق
- تست signed token با اجرای بدون IPC — موفق
- `npm run build` — موفق؛ compile، TypeScript و تولید ۷۵ صفحه کامل شد. پیام‌های عدم اتصال Prisma فقط به نبود PostgreSQL واقعی در محیط بررسی مربوط بود و build با موفقیت تمام شد.

## مراحل استقرار ضروری

پیش از استقرار از دیتابیس نسخه پشتیبان بگیرید و سپس در محیط تولید اجرا کنید:

```bash
npm ci
npm run db:migrate
npm run check:env
node scripts/audit-parspack-env.mjs --env-file .env --site-url https://eloriagallery.ir
npm run build
npm run start
```

پس از بالا آمدن سرویس، یک همگام‌سازی نرخ را اجرا و نتیجه را بررسی کنید:

```bash
npm run metal:diagnose-sync
```

## تنظیمات خارجی که کد به‌تنهایی نمی‌تواند انجام دهد

- در Cloudflare Turnstile هر دو دامنه `eloriagallery.ir` و `www.eloriagallery.ir` باید مجاز باشند و کلید public/secret باید متعلق به یک widget باشند.
- برای ورود ایمیلی، دامنه/فرستنده `ELORIA_EMAIL_FROM` باید در Resend تأیید شده باشد. در غیر این صورت `ELORIA_CUSTOMER_EMAIL_OTP_ENABLED=false` و ورود پیامکی با Kavenegar فعال شود.
- `BRS_API_KEY` معتبر، `ELORIA_DYNAMIC_PRICING_ENABLED=true` و `ELORIA_EMBEDDED_METAL_SYNC_ENABLED=true` برای نرخ زنده لازم‌اند.
- ساعت سرور باید با NTP همگام باشد؛ کد TOTP مدیر به اختلاف زمانی حساس است.

هیچ کلید، رمز، توکن یا اطلاعات محرمانه‌ای در این گزارش یا بسته نهایی قرار داده نشده است.
