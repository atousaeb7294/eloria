# راهنمای کامل نصب و راه‌اندازی الوریا

این نسخه برای Node.js 24، PostgreSQL، Next.js 16 و اجرای PaaS آماده شده است. تمام دستورها را از پوشهٔ اصلی پروژه اجرا کنید.

## ۱. نکات فوری بر اساس تصاویر پارس‌پک

1. `ELORIA_ADMIN_PASSWORD` را حذف کنید و فقط `ELORIA_ADMIN_PASSWORD_HASH` را نگه دارید. وجود هم‌زمان این دو متغیر در نسخهٔ قبلی می‌توانست باعث شود رمز اشتباه خوانده شود. نسخهٔ جدید همیشه هش را در اولویت قرار می‌دهد، ولی حذف متغیر قدیمی همچنان توصیهٔ قطعی است.
2. مقدار `ELORIA_ADMIN_PASSWORD_HASH` باید با `scrypt$...` شروع شود. برای ساخت مقدار تازه، روی رایانهٔ امن اجرا کنید: `npm run admin:password-hash`.
3. `ELORIA_ADMIN_TOTP_SECRET` در تصاویر وجود دارد. همان رشتهٔ Base32 را در Google Authenticator، Microsoft Authenticator یا Aegis وارد کنید. ساعت سرور و موبایل باید دقیق و خودکار باشد.
4. کلیدهای Turnstile وجود دارند. در داشبورد Cloudflare، دامنه‌های `eloriagallery.ir` و در صورت استفاده `www.eloriagallery.ir` را به Widget اضافه و حالت Managed را انتخاب کنید.
5. `RESEND_API_KEY` و `ELORIA_EMAIL_FROM` وجود دارند، اما وجود متغیر به‌تنهایی کافی نیست. دامنهٔ `eloriagallery.ir` و رکوردهای DNS باید در Resend وضعیت Verified داشته باشند و From بهتر است به شکل `Eloria <login@eloriagallery.ir>` باشد.
6. `DATABASE_URL` باید نشانی pooled/runtime و `DIRECT_URL` نشانی مستقیم دیتابیس برای migration باشد. اگر سرویس دیتابیس فقط یک نشانی می‌دهد، استفاده از همان مقدار ممکن است کار کند، اما تنظیم جداگانه پایدارتر است.
7. مقدار `DATABASE_SSL_MODE=require` قابل استفاده است؛ اگر گواهی ارائه‌دهنده کامل پشتیبانی می‌شود، `verify-full` امن‌تر است.
8. `ELORIA_CUSTOMER_WATCHES_ENABLED=false` و `ELORIA_CONTENT_AUTOPILOT_ENABLED=false` یعنی دو قابلیت هوشمند عملاً خاموش‌اند. پس از اولین استقرار سالم، Watches را `true` و Autopilot را با محدودیت روزانه `1` فعال کنید.
9. تا زمانی که درگاه واقعی و callback آن آزمایش نشده، `ELORIA_PAYMENT_ENABLED=false` را نگه دارید.

## ۲. نصب تمیز

پیش‌نیازها: Node.js 24، npm 11، PostgreSQL در دسترس، دامنهٔ HTTPS و حداقل 1GB RAM.

```bash
npm ci
npm run check:env
npm run check:auth
npm run db:migrate
npm run build
npm run start:production
```

در ParsPack، Build Command را `npm ci && npm run build` و Start Command را `npm run start:production` قرار دهید. پس از تغییر هر متغیر محیطی یک Deploy/Restart کامل انجام دهید، زیرا متغیرهای `NEXT_PUBLIC_*` هنگام build در خروجی قرار می‌گیرند.

برای دیتابیس موجود، قبل از migration نسخهٔ پشتیبان بگیرید و ابتدا وضعیت را ببینید:

```bash
npm run db:status
npm run db:migrate
```

`npm run db:seed` را فقط برای دیتابیس تازه یا وقتی دقیقاً می‌دانید seed چه داده‌ای می‌سازد اجرا کنید.

## ۳. آزمون ورود ادمین

1. متغیر رمز قدیمی را حذف و سرویس را rebuild کنید.
2. `npm run check:auth` باید همهٔ سطرها را `OK` نشان دهد.
3. به `/fa/admin/login` بروید، نام کاربری، رمز اصلی، کد شش‌رقمی TOTP و تأیید «من ربات نیستم» را وارد کنید.
4. اگر TOTP رد شد، Time synchronization موبایل را روشن کنید و ساعت سرور را بررسی کنید.
5. اگر کپچا رد شد، hostname دامنه، Site Key و Secret Key متعلق به یک Widget باشند. Token کپچا یک‌بارمصرف است؛ نسخهٔ جدید پس از خطا آن را خودکار تازه می‌کند.

## ۴. آزمون ورود مشتری و ایمیل

1. در Resend، دامنه و DNS باید Verified باشند. API Key باید اجازهٔ Send داشته باشد.
2. From را روی `Eloria <login@eloriagallery.ir>` بگذارید.
3. در `/fa/account/login` شماره موبایل و ایمیل معتبر را وارد و کپچا را کامل کنید.
4. اگر ارسال رد شود، نسخهٔ جدید کد خطای امن را در رویداد امنیتی ثبت می‌کند و OTP ناموفق را از دیتابیس پاک می‌کند؛ بنابراین کد مرده باقی نمی‌ماند.
5. پوشه‌های Spam/Promotions و گزارش Deliverability در Resend را بررسی کنید.
6. کلید تست یا From تأییدنشده برای گیرنده‌های عمومی کار نمی‌کند.

## ۵. کپچا و عبارت Troubleshoot

کادر دیده‌شده متعلق به Cloudflare Turnstile است، نه خطای داخلی سایت. نسخهٔ جدید:

- وضعیت بارگذاری، خطا و موفقیت را شفاف نشان می‌دهد؛
- متن فارسی «تأیید کنید من ربات نیستم» دارد؛
- گزینه‌های feedback/help را غیرفعال می‌کند تا Troubleshoot عادی نمایش داده نشود؛
- در ورود ادمین، ورود مشتری و چت پشتیبانی روی سرور نیز token، action و hostname را اعتبارسنجی می‌کند.

اگر Cloudflare تشخیص دهد مرورگر پشتیبانی نمی‌شود، امکان حذف کامل پیام امنیتی وجود ندارد؛ در آن حالت مرورگر، VPN/DNS و افزونه‌های مسدودکننده باید بررسی شوند.

## ۶. قابلیت‌های هوشمند این نسخه

- انتخاب هوشمند محصولات مرتبط با توجه به جنس، دسته، موجودی و قیمت؛
- هشدارهای مشتری برای تغییر قیمت/موجودی و پیگیری علاقه‌مندی‌ها؛
- قیمت‌گذاری پویا با کنترل انحراف نرخ طلا و نقره؛
- خلاصهٔ مدیریتی، سلامت محتوا، رخدادهای امنیتی و عملیات خودکار محدودشده؛
- تولید افسانهٔ مستقل برای هر اثر با پیوند روشن به افسانهٔ مادر؛
- پروفایل جهان و شخصیت هر اثر: نام ایرانی، نقش، سرزمین، دوره، پوشش، نشانه و prompt تصویری؛
- «استودیوی جهان و افسانه‌ها» در پنل ادمین برای مشاهده و مدیریت پیوستگی جهان؛
- گفت‌وگوی پشتیبانی مقاوم در برابر polling هم‌پوشان و توقف خودکار در تب مخفی.

برای Autopilot ابتدا provider هوش مصنوعی را در شبکهٔ خصوصی تنظیم کنید، سپس `ELORIA_CONTENT_AUTOPILOT_ENABLED=true` و `ELORIA_CONTENT_AUTOPILOT_DAILY_LIMIT=1`. انتشار کاملاً خودکار توصیه نمی‌شود؛ خروجی ابتدا باید Draft و با تأیید ادمین منتشر شود.

## ۷. داستان و هویت ایرانی

افسانهٔ مادر اکنون در `/fa/about#mother-legend` واضح و مرحله‌بندی‌شده است. هر محصول داستانی مستقل دارد که بدون مطالعهٔ کل جهان قابل فهم است، اما از طریق «نشان»، شخصیت و یک پیوند مستقیم به داستان مادر متصل می‌شود. تولید شخصیت و prompt تصویری صریحاً عناصر رومی، یونانی، عربی و فانتزی غربی را رد و معماری، پوشش و نام‌های ایرانی را الزام می‌کند.

## ۸. کنترل سلامت بعد از استقرار

```bash
npm run typecheck
npm run lint
npm run audit:dependencies
npm run test:myths
npm run test:luxury-support
npm run build
```

سپس ورود ادمین، ورود مشتری، ارسال ایمیل واقعی، چت دوطرفه، صفحهٔ محصول، پرداخت آزمایشی و صفحات موبایل را دستی تست کنید. تست واقعی ایمیل، DNS، دیتابیس و پرداخت فقط روی محیط متصل به سرویس‌های شما قابل اثبات است.

## ۹. امنیت و نگهداری

- تمام کلیدهایی که در تصویر یا کانال اشتراکی دیده شده‌اند را پس از تحویل rotate کنید.
- کلید Service Role سوپابیس هرگز نباید `NEXT_PUBLIC_` باشد.
- لاگ‌ها نباید OTP، رمز، token یا کلید کامل را چاپ کنند.
- روزانه backup دیتابیس و دورهٔ نگهداری را آزمایش کنید.
- cronهای قیمت فلز، انقضای سفارش، retention، content health و security alerts را با `CRON_SECRET` اجرا کنید.
- برای جلوگیری از ناسازگاری، همیشه `npm ci` اجرا کنید و `package-lock.json` را همراه پروژه نگه دارید.

## ۱۰. بازگشت امن

پیش از Deploy از دیتابیس snapshot و از نسخهٔ فعلی اپ backup بگیرید. در صورت خطا، نسخهٔ قبلی برنامه را Deploy کنید؛ migration را بدون بررسی schema به‌صورت دستی rollback نکنید. ابتدا لاگ build/runtime و `npm run db:status` را بررسی کنید.
