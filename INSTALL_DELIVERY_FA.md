# راهنمای نصب نسخهٔ تکمیل‌شدهٔ الوریا

این نسخه را در پوشهٔ اصلی پروژه، یعنی `C:\Eloria` نصب کنید. محتویات پوشهٔ `eloria-main` داخل فایل ZIP را استخراج کنید؛ پوشهٔ تو در توی اضافه نسازید، به‌طوری‌که فایل `package.json` دقیقاً در `C:\Eloria\package.json` باشد.

## نصب در PowerShell

PowerShell را باز کنید و این دستورها را، یکی‌یکی، اجرا کنید:

```powershell
Set-Location "C:\Eloria"
Copy-Item ".env.example" ".env" -ErrorAction SilentlyContinue
npm ci
npx prisma generate
npx prisma migrate deploy
npm run build
npm start
```

قبل از اجرای محیط واقعی، مقادیر لازم را فقط در فایل `.env` وارد کنید. نیازی نیست متن یا کدی به «پارس‌پک» اضافه کنید؛ متغیرهای محیطی همان‌جا در `.env` (و سپس در پنل متغیرهای محیطی هاست) ثبت می‌شوند. برای بررسی تنظیمات، از `npm run check:env` استفاده کنید.

## تصاویر موبایل و آتلیه

- پس‌زمینه‌های موبایل را با نام‌های `bg-01.jpeg` تا `bg-09.jpeg` در `public/images/section-backgrounds/` جایگزین کنید. سایت بین آن‌ها به‌صورت تصادفیِ پایدار جابه‌جا می‌شود و تصویر را با نسبت صفحه برش می‌دهد.
- برای هر محصول، تصویر شخصیت و صحنهٔ دنیای آن را در محل ذخیره‌سازی تصاویر مجاز پروژه بارگذاری کنید؛ سپس از پنل «محصولات و موجودی»، در بخش «توضیحات و روایت»، نشانی هر دو تصویر را وارد کنید. این دو تصویر در صفحهٔ محصول و آتلیه/جهان به همان افسانه وصل می‌مانند.
- هر محصول تازه یک افسانهٔ یکتا از کتابخانهٔ ۱۵۴تایی می‌گیرد. در پنل «جهان و افسانه‌ها» نگهبان، نقش، محل و دستور ساخت تصویر همان شخصیت قابل مشاهده است.

## سئو و محتوا

افزونهٔ نامطمئن یا سرویس پولی لازم نیست: پنل «محتوا و سئو» امتیاز واقعی را از توضیح‌های دو زبانه، alt تصاویر، کیفیت مقاله‌ها و تازگی محتوا می‌سازد و موردهای ناقص را نشان می‌دهد. برای هر محصول می‌توانید پیش‌نویس مقاله بسازید، آن را بازبینی و سپس منتشر کنید. نقشهٔ سایت، robots و داده‌های ساختاریافته در پروژه فعال‌اند.

## گیت‌هاب

پس از این‌که سایت محلی درست اجرا شد:

```powershell
Set-Location "C:\Eloria"
git status
git add .
git commit -m "Install Eloria story, SEO and typography update"
git branch -M main
git remote add origin "آدرس-مخزن-گیت‌هاب-خودتان"
git push -u origin main
```

اگر remote از قبل وجود دارد، به‌جای دستور `git remote add` فقط `git remote -v` را بررسی و سپس `git push` را اجرا کنید. فایل `.env` را هرگز به GitHub اضافه نکنید.

## اینماد

مرحلهٔ آزمایشی تأیید اینماد پایان یافته و فایل و متاتگ عددی آن از نسخهٔ عمومی حذف شده‌اند. نشان واقعی اینماد از تنظیمات رسمی خود سایت نمایش داده می‌شود.
