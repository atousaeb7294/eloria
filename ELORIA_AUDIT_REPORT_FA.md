# گزارش نهایی اصلاح و آمادگی انتشار ELORIA

تاریخ: ۴ شهریور ۱۴۰۵ / 26 August 2026
مخزن مبنا: `atousaeb7294/eloria`، commit `48d94a3` و فایل کامل ارسالی کاربر

## نتیجه اجرایی

نقص‌های بحرانی Server Action محصولات، encoding فارسی، endpoint بدون محافظ تولید روایت، Intro، سبد، پذیرش قوانین و ثبت سفارش اصلاح شدند. روایت پنهان دیگر تولید هوشمند ندارد و فقط دستی ثبت می‌شود. بسته فعلی برای **استقرار آزمایشی** آماده است؛ فروش واقعی منوط به تکمیل اطلاعات فروشنده، secretها، آزمون درگاه، backup/restore و سبزشدن CI کامل روی سرور است.

## تغییرات تحویل‌شده

| حوزه | نتیجه |
|---|---|
| روایت محصول | حذف OpenAI، route عمومی و مولدهای موازی؛ ورود دستی عنوان و داستان |
| Intro | ویدئوی دوم 2.67MB، faststart، timeout و بازیابی خطا؛ پخش در هر ورود به خانه و bypass برای reduced-motion/Save-Data |
| سرعت | cache assetهای versioned، lazy rendering بخش‌های پایین و polling بهینه پشتیبانی |
| سبد و checkout | پاک‌کردن کل سبد، پذیرش قوانین نسخه‌دار، honeypot و کنترل origin/نرخ/بدنه |
| تخفیف | `ELORIA50`؛ پنجاه هزار تومان برای اولین خرید واجد شرایط، بررسی سروری بر اساس موبایل و قفل تراکنشی |
| پیشنهاد خرید | پیشنهاد بدون API بر اساس دسته، جنس و بودجه، به‌اضافه قطعه مکمل برای ست |
| پرداخت | محاسبه سمت سرور، idempotency، کنترل شروع پرداخت و verify callback |
| پشتیبانی | گفت‌وگوی داخلی، cookie امضاشده، rate limit، origin، بدنه محدود و ضدربات |
| تماس | جایگاه امن اینستاگرام، تلگرام و بله با allowlist دامنه |
| قوانین | شرایط خرید، حریم خصوصی، ارسال، عدم مرجوعی مشروط طلا و حفظ حقوق عیب/آسیب/مغایرت |
| اینترنت ملی | حالت داخلی، media داخلی S3 پارس‌پک و asset و فونت self-hosted |
| استقرار | Docker build args، قرارداد env و راهنمای پارس‌پک |

## کنترل کیفیت انجام‌شده

- verifier نهایی بسته: ۲۵ کنترل، صفر خطا.
- TypeScript (`tsc --noEmit`): موفق.
- ESLint کل پروژه: موفق، صفر خطا.
- Next.js 16.3.1 production build: موفق؛ ۶۹ صفحه ایستا/SSG تولید و مسیرهای پویا و API ثبت شدند.
- امنیت ایستا: ۳۷ کنترل hardening، صفر خطا.
- کیفیت حرفه‌ای: همه کنترل‌ها پس از اصلاح لینک خارجی Checkout موفق شدند.
- زنجیره تأمین CI، idempotency سفارش، توکن‌های امضاشده، قیمت‌گذاری و محافظ نرخ فلز: موفق.
- اسکن conflict marker، نویسه جایگزین UTF-8، mojibake و فایل‌های محیطی واقعی: موفق.
- بسته نهایی فاقد `.env` واقعی، لاگ، backup، patch، build output و `node_modules` است.

تست‌هایی که به PostgreSQL واقعی، زرین‌پال، سرویس پیامک و مرورگر staging نیاز دارند باید پس از ساخت محیط آزمایشی اجرا شوند. فروش واقعی تا موفقیت migration، تست پرداخت sandbox، هم‌زمانی موجودی و بازیابی backup فعال نشود.

## موارد لازم از مالک فروشگاه

1. نام حقوقی/صنفی، نشانی، تلفن یا ایمیل واقعی و لینک شبکه‌های اجتماعی را در `.env.production` وارد کند؛ هیچ اطلاعاتی اختراع نشده است.
2. زرین‌پال، کاوه‌نگار، دامنه، DNS، secretهای مستقل و TOTP مدیر را تنظیم کند.
3. Bucket عمومی تصاویر و Access Key محدود پارس‌پک را بسازد.
4. backup خودکار و بازیابی واقعی را آزمایش کند.
5. متن حقوقی را متناسب با نوع دقیق محصولات با مشاور حقوقی ایران تأیید کند.

## مرز امنیت و دسترس‌پذیری

حالت `ELORIA_DOMESTIC_NETWORK_MODE=true` برای تداوم خرید بدون اینترنت خارجی، Turnstile را کنار می‌گذارد؛ کنترل‌های داخلی باقی می‌مانند، اما این یک trade-off آگاهانه است. در حالت عادی بهتر است هر دو flag داخلی `false` و Turnstile فعال باشد. هیچ معماری نمی‌تواند دسترسی شبکه ملی را مطلقاً تضمین کند؛ DNS، اپراتور و مرکز داده باید عملاً از چند شبکه ایران آزمایش شوند.

## تصمیم Go-Live

تا تکمیل اطلاعات واقعی، CI، پرداخت آزمایشی، تست هم‌زمانی موجودی و restore backup، دو flag تجارت و پرداخت باید `false` بمانند. سپس فعال‌سازی مرحله‌ای و پایش خطا و تأخیر انجام شود.
