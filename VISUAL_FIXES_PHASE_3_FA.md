# گزارش اصلاحات ظاهری مرحله سوم ELORIA

## محدوده این مرحله

این مرحله روی مسیر کامل مشاهده و خرید محصول متمرکز است:

- صفحه تمام محصولات
- فیلتر محصولات در موبایل
- کارت محصولات
- سبد خرید
- تکمیل سفارش
- حالت‌های Loading و Error

## اصلاحات انجام‌شده

### ۱. کاتالوگ و فیلتر محصولات

- فیلترهای پیشرفته در موبایل به حالت باز و بسته‌شونده درآمدند.
- تعداد فیلترهای فعال روی دکمه فیلتر نمایش داده می‌شود.
- جست‌وجو همیشه در دسترس باقی می‌ماند.
- دکمه جست‌وجوی مستقیم داخل فیلد اضافه شد.
- بازه قیمت نامعتبر شناسایی و با پیام واضح نمایش داده می‌شود.
- دکمه اعمال فیلتر هنگام نامعتبر بودن بازه قیمت غیرفعال می‌شود.
- فاصله کارت‌ها در موبایل و تبلت اصلاح شد.
- تعداد نتایج در یک نوار واضح‌تر نمایش داده می‌شود.
- در حالت بدون نتیجه، دکمه «نمایش همه محصولات» اضافه شد.

### ۲. کارت محصول

- شعاع گوشه‌ها، فاصله داخلی و ارتفاع عنوان در موبایل اصلاح شد.
- دکمه مشاهده مشخصات در موبایل کوتاه‌تر و متناسب‌تر شد.
- کارت‌ها در عرض‌های کوچک فضای عمودی کمتری اشغال می‌کنند.
- تصاویر خارجی دیتابیس به‌صورت `unoptimized` نمایش داده می‌شوند تا به تنظیم دامنه ثابت در Next.js وابسته نباشند.

### ۳. مسیر مراحل خرید

نوار سه‌مرحله‌ای جدید اضافه شد:

1. سبد خرید
2. اطلاعات ارسال
3. پرداخت

مرحله فعال و مراحل تکمیل‌شده به‌صورت بصری مشخص می‌شوند.

### ۴. سبد خرید موبایل

- تصویر محصول در موبایل از حالت تمام‌عرض به اندازه فشرده ۹۶ پیکسل تغییر کرد.
- عنوان، مدل، جنس، تعداد و قیمت خواناتر شدند.
- بخش مبلغ هر محصول در صفحه کوچک بهتر چیده می‌شود.
- نوار ثابت پایین صفحه اضافه شد که شامل مبلغ کل و دکمه ادامه خرید است.
- فضای پایین صفحه برای جلوگیری از پوشانده‌شدن محتوا توسط نوار ثابت افزایش یافت.
- Safe Area دستگاه‌های دارای نوار پایین یا بریدگی صفحه لحاظ شد.

### ۵. تکمیل سفارش موبایل

- نوار مراحل خرید در بالای صفحه اضافه شد.
- کنتراست Focus فیلدهای فرم افزایش یافت.
- نوار ثابت پایین صفحه شامل مبلغ قابل پرداخت و دکمه ثبت سفارش اضافه شد.
- دکمه بازگشت به سبد خرید داخل نوار ثابت قرار گرفت.
- دکمه‌های تکراری خلاصه سفارش در موبایل مخفی و در دسکتاپ حفظ شدند.
- پس از ایجاد سفارش، مرحله پرداخت در نوار مراحل فعال می‌شود.

### ۶. Loading و Error

برای مسیرهای زیر صفحه Loading و Error اختصاصی اضافه شد:

- `/[locale]/products`
- `/[locale]/cart`
- `/[locale]/checkout`

صفحات خطا شامل این موارد هستند:

- پیام فارسی و انگلیسی
- دکمه تلاش مجدد
- لینک بازگشت به محصولات
- نمایش Error ID در صورت وجود

## فایل‌های اصلی تغییرکرده

- `src/app/[locale]/products/page.tsx`
- `src/components/product-catalog-filters.tsx`
- `src/components/catalog-product-card.tsx`
- `src/components/cart-page-client.tsx`
- `src/components/checkout-page-client.tsx`
- `src/components/product-gallery.tsx`

## فایل‌های جدید

- `src/components/purchase-progress.tsx`
- `src/components/commerce-page-loading.tsx`
- `src/components/commerce-route-error.tsx`
- `src/app/[locale]/products/loading.tsx`
- `src/app/[locale]/products/error.tsx`
- `src/app/[locale]/cart/loading.tsx`
- `src/app/[locale]/cart/error.tsx`
- `src/app/[locale]/checkout/loading.tsx`
- `src/app/[locale]/checkout/error.tsx`

## بررسی انجام‌شده

- ۱۲۱ فایل TypeScript و TSX از نظر Syntax بررسی شدند.
- مسیر Import فایل‌های تغییرکرده بررسی شد.
- Patch از اختلاف نسخه Phase 2 و Phase 3 تولید شد.
- نصب کامل وابستگی‌ها در محیط بررسی به دلیل نبود پکیج `zod-validation-error@4.0.2` در Registry داخلی متوقف شد؛ این محدودیت مربوط به محیط بررسی است، نه سورس پروژه.

## تست پیشنهادی روی سیستم اصلی

```powershell
npm install
npm run lint
npm run build
npm run dev
```

سپس این مسیرها در عرض موبایل و دسکتاپ بررسی شوند:

```text
/fa/products
/en/products
/fa/cart
/en/cart
/fa/checkout
/en/checkout
```
