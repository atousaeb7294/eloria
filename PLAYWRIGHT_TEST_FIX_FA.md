# اصلاح تست Playwright و مسیر Hero

- لینک مستقیم `#hero` پیش از Hydration، Intro را مخفی می‌کند تا کلیک‌های Hero و هدر مسدود نشوند.
- تست منوی موبایل روی هدر داخلی اجرا می‌شود؛ منوی مینیمال Hero عمداً فقط داستان و تماس را نمایش می‌دهد.
- Playwright از Google Chrome نصب‌شده روی ویندوز استفاده می‌کند.
- ضبط Video خاموش است؛ Chromium و FFmpeg دانلود نمی‌شوند.
