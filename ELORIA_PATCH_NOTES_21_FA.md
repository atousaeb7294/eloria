# اصلاحات نسخه 21 — هوشمندی امضادار + خرید مهمان + پایداری Production

این بسته بر پایه `eloria-main (21).zip` ساخته شده است.

## تجربه امضادار «اثر من را پیدا کن»

- CTA اصلی صفحه نخست به «اثر من را پیدا کن» تبدیل شد.
- راهنمای انتخاب به تجربه تمام‌صفحه چندمرحله‌ای تبدیل شد.
- انتخاب برای خود / هدیه، حال‌وهوای اثر، جنس، نوع اثر، سقف بودجه و نشانه آزاد پشتیبانی می‌شود.
- ورودی آزاد محدود به مثال‌هایی مثل «سنگ سبز» نیست و با جست‌وجوی کاتالوگ روی نام، توضیح، روایت و اطلاعات ثبت‌شده کار می‌کند.
- API جدید `/api/smart-selection` از آثار واقعی و موجود فروشگاه تا 3 پیشنهاد برمی‌گرداند و برای هر انتخاب دلیل کوتاه نمایش می‌دهد.
- اگر جست‌وجوی توصیفی بیش از حد محدود باشد، سیستم فقط بخش توصیفی را آزادتر می‌کند و جنس/نوع/بودجه را حفظ می‌کند.
- نتیجه آخر روی همان مرورگر ذخیره می‌شود تا تجربه مهمان از بین نرود.
- endpoint جدید دارای trusted-origin check و rate limit است.

## هدیه‌یاب

- حالت «برای هدیه» داخل همان تجربه امضادار ادغام شده تا یک مسیر جدا و تکراری ساخته نشود.

## خرید مهمان

- backend پروژه از قبل سفارش بدون customer session را می‌پذیرفت و برای شروع پرداخت مهمان توکن امن اختصاصی داشت.
- در Checkout حالا به‌صورت واضح نمایش داده می‌شود که ساخت حساب برای خرید و پرداخت اجباری نیست.
- ایمیل همچنان اختیاری است و اطلاعات تحویل/شماره موبایل برای سفارش استفاده می‌شود.

## ورود مشتری با ایمیل

- production environment checker اصلاح شد: Kavenegar فقط وقتی SMS OTP فعال باشد اجباری است.
- وقتی SMS OTP خاموش است، بررسی Production روی `RESEND_API_KEY` و `ELORIA_EMAIL_FROM` انجام می‌شود.
- برای فعال بودن خود قابلیت در Production همچنان `ELORIA_CUSTOMER_AUTH_ENABLED=true` لازم است.
- مقدار secretها داخل این بسته قرار داده نشده و نباید داخل Git ذخیره شوند.

## قیمت لحظه‌ای فلزات

- startup probe به `/api/health?mode=live` منتقل شد تا Sync منتظر آماده‌شدن خود سرور بماند و به health dependencyهای غیرضروری گیر نکند.
- انتقال امن اولیه `ELORIA_TEST_SEED -> BRS_API` اضافه شد: فقط مقایسه درصدی با seed تستی در اولین انتقال کنار گذاشته می‌شود؛ حدود مطلق ایمنی قیمت همچنان اعمال می‌شوند.
- پس از اولین نرخ واقعی، تغییرات بعدی BRS دوباره تحت کنترل deviation عادی هستند.

## مواردی که دست نخورده‌اند

- فرمول‌های مالی سفارش و پرداخت
- زرین‌پال
- رزرو موجودی
- Prisma schema و migrationها
- admin session / TOTP
- ثبت رویدادهای امنیتی و کنترل anomaly قیمت

## بررسی انجام‌شده در محیط ساخت بسته

- `node --check scripts/start-production.mjs` : PASS
- Parse/Transpile syntax برای تمام فایل‌های TS/TSX تغییرکرده با TypeScript compiler : PASS
- Build کامل Next.js در این محیط اجرا نشد، چون dependencyهای پروژه داخل ZIP موجود نبودند و `npm ci` در محیط ساخت به timeout خورد.

## متغیرهای مهم Production

برای ورود ایمیلی:
- `ELORIA_CUSTOMER_AUTH_ENABLED=true`
- `ELORIA_CUSTOMER_AUTH_SECRET`
- `RESEND_API_KEY`
- `ELORIA_EMAIL_FROM`
- تنظیمات Turnstile مربوط به auth در صورت فعال بودن challenge

برای قیمت زنده:
- `ELORIA_DYNAMIC_PRICING_ENABLED=true`
- `BRS_API_KEY`
- `CRON_SECRET`
- `ELORIA_EMBEDDED_METAL_SYNC_ENABLED=true`
- حدود ایمنی GOLD/SILVER که قبلاً در ParsPack تعریف شده‌اند

هیچ مقدار secret در این فایل ثبت نشده است.
